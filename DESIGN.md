# UnLhaRe の実装設計

## 目的と境界

実装は `UnLha32Re/` にあり、UNLHA32.DLL 3.00.0.5 の x86 ABI と観測可能な動作を再現する Windows DLL `UNLHA32RE.DLL` を構築する。完全互換は目標であり、全入力・全副作用の一致を示すものではない。確認範囲、既知の差異、データ保護上の例外は [プロジェクト README](UnLha32Re/README.md) を正本とする。

## 主要コンポーネント

| 場所 | 責務 |
| --- | --- |
| `UnLha32Re/include/`、`src/unlhare.def` | 公開構造体・定数・宣言、エクスポート名と序数、SFX フッター形式 |
| `UnLha32Re/src/unlhare.cpp` | A/W API、コマンド解釈、書庫ハンドルと検索、メモリ操作、文字コード変換、エラー・共有状態、設定と Windows ファイル操作 |
| `UnLha32Re/third_party/lha/src/` | Windows/DLL 対応と互換修正を含む LHa 系のヘッダー読み書き、圧縮・復号処理 |
| `UnLha32Re/src/lha_progress.c` | C エンジンから DLL 側への進捗通知、累計・項目状態、中断の橋渡し |
| `UnLha32Re/src/unlhare.rc`、`sfx/` | DLL リソースと設定画面、自己解凍ローダー |
| `UnLha32Re/tests/`、`scripts/` | API 観測プローブ、原版との差分比較、fixture 生成、ビルドと試験の実行・分離 |

## データフローと状態

1. ホストが公開 API を呼び、DLL が引数・文字コード・設定・処理中状態を解釈する。コマンド API は処理対象とオプションを組み立て、書庫操作と LHa エンジンへ接続する。
2. 書庫ヘッダーと本文を読み書きし、列挙コールバックで対象の選択や名前の変更を反映する。進捗は通知ブリッジを通じてホストへ返り、中断は戻り値・ログ・ファイル状態に反映される。
3. ハンドル API は `ArcHandleContext` に書庫情報と検索状態を保持し、`FindFirst` / `FindNext` と情報取得 API がそれを参照する。呼び出し間の検索位置や直前情報も互換対象になる。
4. 処理結果はファイル、メモリバッファ、ログ、メタデータ、互換エラーとシステムエラーとして返る。本文だけでなく、これらの副作用を試験で比較する。

コードページ、Unicode モード、処理中状態、登録通知などには DLL 内の共有状態がある。独立セッションを自由に並列実行する設計ではなく、再入拒否と状態遷移も互換契約に含む。処理開始後の `OpenArchive` 失敗で処理中状態が残る経路も、原版に合わせて実装されている。

ANSI API の文字コードと書庫内名のコードページを分離し、Wide 入力や Unicode 拡張ヘッダーを扱う。設定保存は HKCU の `Software\ArchiverDll\Common` / `UNLHA32` 系を利用する。試験では HKCU を一時キーへ差し替え、製品の設定保存動作と利用者の設定を分離する。

`e/x` の既存ファイルは、日時・対象選択、通常の上書き確認、特殊属性の確認の順に判定する。
通常上書き・特殊属性・親ディレクトリ作成の一括選択は別々に保持し、次の命令で初期化する。C++ 側の確認結果は
新規保存先・確認済みの既存保存先・除外・中断を区別して C エンジンへ渡す。
中断は C++ の一時オブジェクトを破棄した後に共通の入力解放へ進め、事前判定後に現れた
未確認の同名ファイルについては C 側の上書き拒否を維持する。
存在しない親ディレクトリの確認は対象選択後に行い、実際の作成と日時復元は C エンジンに残す。
明示ディレクトリ項目は `Melted` 記録後に確認し、拒否時は作成せず次項目へ進む。
`-m/-y` による共通設定と `-jyo/-jyc/-jyn` による個別設定を命令内の順序どおりに適用し、
上書き・ディレクトリ作成・別名入力の省略を別々に保持する。`-m` は別名入力の設定を変更しない。
通常上書き・親作成の拒否後は、設定に応じて別名の確認と OS 標準の保存画面を開く。
選択後は保存先の存在・日時・保護属性・親を再判定し、元の項目の列挙通知は繰り返さない。
保存画面の後は呼び出し前の作業ディレクトリを復元する。明示ディレクトリ項目では
原版どおり選択された別名も作成せず、その項目を終了する。特殊属性の拒否では別名を問い合わせない。
通常ファイルの容量確認は親作成後・ファイル作成前に行い、呼び出し元が利用できる空き容量へ既存ファイルの
サイズを加算する。必要量は項目サイズと予約容量で判定する。拒否は項目のスキップ、取消は
`ERROR_DISK_SPACE` と入力解放へ接続する。`-f/-jyk/-jd` と保存設定は命令ごとに初期化する。
特殊属性の上書きを許可すると原版どおり属性を通常へ戻し、後続の容量拒否でもこの変更は残す。
ファイル作成は既存先を先に削除せず一度だけ試み、失敗時は既存本文を保持して `Skipped` を記録する。
`-jse1/2` では作成失敗を `ERROR_FILE_OPEN` と実際のシステムエラーへ変換して終了し、
`-gm1` の指定がなければエラー画面を出す。C++ の診断処理から戻った後に C 側で入力を解放する。
`-jse2` は日時・存在・保護属性・隠し／システム属性・通常ファイルの親作成拒否による除外も停止へ変換する。
明示ディレクトリ項目の作成拒否は原版どおり停止に変換しない。実際の親作成失敗と既存ファイルの
情報取得用オープン失敗は `-jse` に関係なく停止し、それぞれ `CheckDirectory` と
`MyGetFileTimeInfo` の診断・実システムエラーを返す。これらと出力作成失敗の状態は共通構造で保持し、命令開始時に消去する。
選択停止のシステムエラーには原版の前処理状態を反映し、既存ファイルのマッピング成功時の87、
空ファイル／マッピング無効時の0、存在照会の失敗値、親作成拒否の1223を区別する。

## 不変条件と採用済みの判断

- **バイナリ互換を優先する。** x86、stdcall、1 バイト境界の公開構造体、原版の名前・序数とバージョンリソースを維持する。これにより既存ホストとの接続を保つ一方、型幅や API 形状の自由な変更には制約がある。
- **既存 LHa 系コードを組み込む。** UnLha64x 由来のラッパーと LHa エンジンを拡張している。原版 DLL は比較対象であり製品のリンク先ではない。既存コードを活用する代わりに派生元の配布条件を引き継ぐ。詳細は [第三者告知](UnLha32Re/THIRD_PARTY_NOTICES.md) に集約する。
- **データ保護を互換性より優先する条件がある。** 未格納入力や拒否された既存項目の保持、破損しない LH3 出力などは意図的な差異であり、原版との単純一致ではなく保持・復元の試験で保証する。例外の一覧はプロジェクト README を参照する。
- **API ごとのエラー契約を維持する。** メモリ展開はデータ CRC 不一致を必ずしもエラーにしないため、その成功だけでは整合性を保証できない。CRC 検査は `UnlhaCheckArchive` の契約で別途扱う。
- **SFX は展開エンジンを内包する。** ローダー、書庫、DLL と位置・サイズを持つフッターを使い、実行時にフッターの範囲を検査して DLL を一時ファイルへ取り出す。`UnlhaW` で自身の書庫を展開後、DLL を解放して一時ファイルを削除する。外部 DLL の配置に依存しない代わりに一時ファイルの作成・ロードが必要となる。
- **試験の表示と設定を隔離する。** `DesktopRunner` と互換試験プログラムは非表示デスクトップを使い、分離失敗時は停止する。ダイアログを含む実動作を検証できる一方、これはファイルやネットワークを制限するサンドボックスではない。

## ビルドと検証の位置付け

`UnLhaRe.sln` は DLL、SFX ローダー、互換試験、DesktopRunner の C++ プロジェクトをまとめる。DLL はビルド時に SFX プロジェクトを参照する。生成物は `artifacts/`、中間生成物は `build/` に分離される。

`scripts/test.ps1` はビルド、PE・エクスポート・バージョン検査、書庫 fixture に対する API 比較と機能別試験を組み合わせる。比較元 DLL と一部 fixture は追跡対象外の `sample/` に依存する。固定された期待値を使う試験とライブ比較は別に扱われる。実行コマンドと作業時の検証規約は [AGENTS.md](AGENTS.md)、隔離実行は [検証用デスクトップ](UnLha32Re/docs/testing-desktop.md) を参照する。
