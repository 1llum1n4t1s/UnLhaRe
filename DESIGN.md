# UnLhaRe の実装設計

## 目的と境界

互換版の実装は `UnLha32Re/` にあり、UNLHA32.DLL 3.00.0.5 の x86 ABI と観測可能な動作を再現する Windows DLL `UNLHA32RE.DLL` を構築する。完全互換は目標であり、全入力・全副作用の一致を示すものではない。確認範囲、既知の差異、データ保護上の例外は [プロジェクト README](UnLha32Re/README.md) を正本とする。

`UnLhaRe/` はRust 1.98.1 / edition 2024による独立した近代化版。Windows/macOSのx64/ARM64を対象とし、旧x86 ABIを引き継がない。利用手順と機能境界は [新版README](UnLhaRe/README.md)。以下の互換版の設計とは実装と試験入口を分離する。

## 近代化版の構成

| 場所 | 責務 |
| --- | --- |
| `UnLhaRe/src/lib.rs` | 公開Rust API、64bitサイズと呼び出し単位のLimits、入力ディレクトリ列挙 |
| `UnLhaRe/src/reader.rs` | delharcによるヘッダー読取・一覧・CRC検査・展開、cap-stdによる出力先基点の操作 |
| `UnLhaRe/src/writer.rs` | oxiarc-lzhufによる圧縮、レベル2/Unicode/64bitサイズ拡張、新規書庫の確定 |
| `UnLhaRe/src/pathname.rs` | UTF-8/UTF-16/旧コードページの解釈と共通のファイル名制約 |
| `UnLhaRe/src/ffi.rs`、`include/unlhare.h` | C ABI 1、UTF-8文字列、固定幅整数、呼び出し元所有のバッファ、スレッド別エラー |
| `UnLhaRe/src/main.rs` | create/list/test/extract CLI |
| `UnLhaRe/src/operation.rs`、`UnLhaRe/src/ffi/app.rs` | API level 2の進捗・キャンセル、選択操作と上限指定JSON |
| `UnLhaRe/bindings/dotnet/` | Native AOT対応C# API、CPU別DLLとライセンスのNuGet同梱 |
| `UnLhaRe/tests/`、`UnLhaRe/bindings/dotnet/tests/`、`UnLhaRe/scripts/` | Rust・C・.NETの契約試験、ローカルbundle作成 |
| `.github/workflows/modern.yml`、`.github/workflows/publish-unlhare-nuget.yml` | 4環境のネイティブ検証、Native AOT試験、署名済みDLLからのNuGet作成・検証・公開 |

### 近代化版のデータフローと契約

Rust APIは呼び出し単位の入力・上限・進捗コールバックをreaderまたはwriterへ渡す。API level 2のC入口は未知フィールドを拒否するJSONを同じ操作へ変換し、従来のABI 1関数は既定上限を使う経路として維持する。書庫の選択展開は名前の完全一致で本文を選ぶ一方、ヘッダー走査と上限判定は未選択項目を含む書庫全体へ適用する。

進捗コールバックは操作を実行したスレッドで同期呼び出しし、Rustの`false`またはCの非0をキャンセルとして扱う。圧縮器が単一項目を処理している間は中断できないため、その前後をキャンセル点とする。.NETラッパーはABI 1とAPI level 2を検査してJSONを受け渡し、GCHandleをnative呼び出し中だけ保持する。managed例外をC境界の外へ出さず、nativeから戻った後に再送出する。

新版は操作ごとにデコーダーと状態を所有し、レジストリやホストのシグナル設定を変更しない。ディレクトリ一括圧縮と展開では基点をシンボリックリンク非追跡で開き、cap-stdのディレクトリハンドル配下だけを名前解決する。個別の圧縮元ファイルは非追跡で開いたハンドルの同一性を再検査する。読取書庫は事前検査から復号まで同じファイルハンドルを保持する。圧縮元は一度の読込みでCRCを計算し、同一書庫内ではエンコーダーの作業領域と圧縮バッファを再利用する。LH0と圧縮後の方が大きい項目は読込み済み入力を直接書き込む。作成先と展開先の既存ファイルを置換しない。作成は一時書庫の完成後に確定、展開は各ファイルのサイズ・CRC一致後に同一ファイルシステム内のhard linkで確定する。hard linkが使えなければ親ディレクトリのハンドルを基点とするno-replace renameで確定し、確定前の失敗時は一時ファイルだけを削除する。後続項目で失敗した場合、先に確定したファイルは残る。サイズ表現はu64だが圧縮は1項目をメモリに保持するため、既定の容量上限を設ける。読取書庫の実ファイルサイズと保持メタデータにも固定の余裕上限を設け、圧縮率や項目名を悪用した資源枯渇を拒否する。

公開Rust API・C ABI・.NET APIは特定のアプリやUIフレームワークへ依存しない。進捗・キャンセル・選択項目・Limitsは呼び出し単位で受け取り、設定保存、UIへの通知の転送・間引き、上書き確認、ファイル関連付け、製品更新は利用アプリが担当する。Lhamielは利用例の一つであり、その設定値やUIの型を公開契約に含めない。

開発中のAPI level 3はABI 1の追加exportとして提供する。一覧は走査中にキャンセル可能で、同期JSON結果通知によりサイズ照会のための二重走査を避ける。圧縮の結果通知版は入力I/Oエラーだけをスキップして入力順の成否を返し、従来の圧縮APIは全件成功を要求する。結果通知は書庫確定後でありキャンセルできない。書庫内名は圧縮入力の境界で区切りを正規化し、その後に安全性と重複を検証する。エントリの更新日時はUnix秒として公開し、タイムゾーンのないDOS日時は実行環境のローカル時間として解釈する。任意の日時復元はCRC検査済みの通常ファイルにのみ、ステージングハンドル経由・公開前に行う。ディレクトリ日時と既存出力のメタデータは変更しない。.NETの従来メソッドはAPI level 2、新機能を使う追加メソッドはAPI level 3を要求する。

NuGetはGitHub Releaseの署名済みWindows DLLを収録し、任意のWindows .NET 10アプリから参照できる。利用アプリは版とlockfileを固定する。Lhamiel向けの公開後の参照同期はvava.config.jsonで設定するが、この設定や同期スクリプトはライブラリの実行時依存ではない。各製品本体の配信はそれぞれのリリース工程で行う。

元のx86ソースやDLLを新ライブラリへリンクしない。Rust依存はCargo.lockで固定し、新版の第三者告知とライセンス原文をbundleに収録する。

## 互換版の主要コンポーネント

| 場所 | 責務 |
| --- | --- |
| `UnLha32Re/include/`、`src/unlhare.def` | 公開構造体・定数・宣言、エクスポート名と序数、SFX フッター形式 |
| `UnLha32Re/src/unlhare.cpp` | A/W API、コマンド解釈、書庫ハンドルと検索、メモリ操作、文字コード変換、エラー・共有状態、設定と Windows ファイル操作 |
| `UnLha32Re/third_party/lha/src/` | Windows/DLL 対応と互換修正を含む LHa 系のヘッダー読み書き、圧縮・復号処理 |
| `UnLha32Re/src/lha_progress.c` | C エンジンから DLL 側への進捗通知、累計・項目状態、中断の橋渡し |
| `UnLha32Re/src/unlhare.rc`、`sfx/` | DLL リソースと設定画面、自己解凍ローダー |
| `UnLha32Re/tests/`、`scripts/` | API 観測プローブ、原版との差分比較、fixture 生成、ビルドと試験の実行・分離 |

## 互換版のデータフローと状態

1. ホストが公開 API を呼び、DLL が引数・文字コード・設定・処理中状態を解釈する。コマンド API は処理対象とオプションを組み立て、書庫操作と LHa エンジンへ接続する。
2. 書庫ヘッダーと本文を読み書きし、列挙コールバックで対象の選択や名前の変更を反映する。進捗は通知ブリッジを通じてホストへ返り、中断は戻り値・ログ・ファイル状態に反映される。
3. ハンドル API は `ArcHandleContext` に書庫情報と検索状態を保持し、`FindFirst` / `FindNext` と情報取得 API がそれを参照する。呼び出し間の検索位置や直前情報も互換対象になる。
4. 処理結果はファイル、メモリバッファ、ログ、メタデータ、互換エラーとシステムエラーとして返る。本文だけでなく、これらの副作用を試験で比較する。

コードページ、Unicode モード、処理中状態、登録通知などには DLL 内の共有状態がある。独立セッションを自由に並列実行する設計ではなく、再入拒否と状態遷移も互換契約に含む。`DllRunningScope` は処理中フラグを原子的に獲得し、競合した呼び出しを待機させず拒否する。W API から共通処理への委譲は内部関数を使い、公開入口のガードを二重取得しない。処理スコープの終了による解放と書庫セッションの寿命は別であり、処理開始後の `OpenArchive` 失敗で処理中状態が残る経路も、原版に合わせて実装されている。

ANSI API の文字コードと書庫内名のコードページを分離し、Wide 入力や Unicode 拡張ヘッダーを扱う。設定保存は HKCU の `Software\ArchiverDll\Common` / `UNLHA32` 系を利用する。試験では HKCU を一時キーへ差し替え、製品の設定保存動作と利用者の設定を分離する。

非 ANSI パスを含む Wide 圧縮・注釈命令も、実パスを UTF-8 で共通処理へ渡す。
入力を別名ファイルへ複写せず、元の名前に対する除外・既存項目照合・列挙通知の書き換えを維持する。
環境変数由来の作業書庫は Wide API で作成し、コマンド単位の UTF-8 パス登録へ加える。
ファイル操作用の符号化を ANSI 通知へ直接公開せず、通知用コードページへ変換する。
`a/u/m` の除外は新規項目に適用し、旧項目の更新は除外名でも処理する。`f` は旧項目にも除外を適用する。
旧項目かどうかの判定は格納名・書庫順の照合と共有し、通常処理と全体進捗の事前走査で同じ規則を使う。
Wide の事前探索は本処理と同じスイッチ・応答ファイル解析を使い、保存設定の遅延状態を進めずに参照する。
既存書庫の置換は Windows の置換操作で確定する。TEMP が別ドライブの場合は保存先側の未公開
一時ファイルへコピーして flush し、全工程の成功後にだけ元書庫を置き換える。
コマンドが作成した LHT/LHC 一時ファイルだけを終了時に清掃し、失敗・中断時の元書庫と入力を保持する。

`e/x` の既存ファイルは、日時・対象選択、通常の上書き確認、特殊属性の確認の順に判定する。
通常上書き・特殊属性・親ディレクトリ作成の一括選択は別々に保持し、次の命令で初期化する。C++ 側の確認結果は
新規保存先・確認済みの既存保存先・除外・中断を区別して C エンジンへ渡す。
中断は C++ の一時オブジェクトを破棄した後に共通の入力解放へ進め、事前判定後に現れた
未確認の同名ファイルについては C 側の上書き拒否を維持する。
存在しない親ディレクトリの確認は対象選択後に行い、実際の作成と日時復元は C エンジンに残す。
明示ディレクトリ項目は `Melted` 記録後に確認し、拒否時は作成せず次項目へ進む。
`-m/-y` による共通設定と `-jyo/-jyc/-jyn` による個別設定を命令内の順序どおりに適用し、
上書き・ディレクトリ作成・別名入力の省略を別々に保持する。`-m` は別名入力の設定を変更しない。
通常上書き・親作成の拒否後は、設定に応じて別名の確認と OS 標準の保存画面を開く。
選択後は保存先の存在・日時・保護属性・親を再判定し、元の項目の列挙通知は繰り返さない。
保存画面の後は呼び出し前の作業ディレクトリを復元する。明示ディレクトリ項目では
原版どおり選択された別名も作成せず、その項目を終了する。特殊属性の拒否では別名を問い合わせない。
通常ファイルの容量確認は親作成後・ファイル作成前に行い、呼び出し元が利用できる空き容量へ既存ファイルの
サイズを加算する。必要量は項目サイズと予約容量で判定する。拒否は項目のスキップ、取消は
`ERROR_DISK_SPACE` と入力解放へ接続する。`-f/-jyk/-jd` と保存設定は命令ごとに初期化する。
特殊属性の上書きを許可すると原版どおり属性を通常へ戻し、後続の容量拒否でもこの変更は残す。
ファイル作成は既存先を先に削除せず一度だけ試み、失敗時は既存本文を保持して `Skipped` を記録する。
`-jse1/2` では作成失敗を `ERROR_FILE_OPEN` と実際のシステムエラーへ変換して終了し、
`-gm1` の指定がなければエラー画面を出す。C++ の診断処理から戻った後に C 側で入力を解放する。
`-jse2` は日時・存在・保護属性・隠し／システム属性・通常ファイルの親作成拒否による除外も停止へ変換する。
明示ディレクトリ項目の作成拒否は原版どおり停止に変換しない。実際の親作成失敗と既存ファイルの
情報取得用オープン失敗は `-jse` に関係なく停止し、それぞれ `CheckDirectory` と
`MyGetFileTimeInfo` の診断・実システムエラーを返す。これらと出力作成失敗の状態は共通構造で保持し、命令開始時に消去する。
選択停止のシステムエラーには原版の前処理状態を反映し、既存ファイルのマッピング成功時の87、
空ファイル／マッピング無効時の0、存在照会の失敗値、親作成拒否の1223を区別する。

新規 `a/u/m` と新規出力先への `j` では、出力先の親が存在しないときに一時書庫へ進まず、
作成失敗のパス・システムエラーを共通状態へ記録して `ERROR_ARC_FILE_OPEN` として公開する。
この失敗では親・出力・一時ファイルを作成せず、入力とカレントディレクトリを保持する。
親が存在する通常の作成経路、既存書庫の更新経路にはこの状態を適用しない。

注釈命令 `c` は選択された項目だけで注釈ファイルを読み込むか、既存注釈を初期値にした入力画面を開く。
対話入力の空文字での受理は注釈削除、取消はその項目の元ヘッダーと本文の保持とする。
未選択項目では注釈入力を要求せず、存在しない注釈ファイルも開かない。

`c/n/y` と既存連結先への `j` は、入力書庫を開いたハンドルから作成日時を取得し、
置換前の一時出力へ復元する。本文の変更がない対象なしの成功でも、この日時を保持する。
`-jsf` は命令ごとに有効へ初期化し、0／- は無効、1／+ は有効、省略・範囲外は現在値の反転とする。
有効時は原版の現行 Windows と同じローカル日時範囲へ補正し、NTFS は1980年より前を保持する。
新規連結先には既存書庫の日時を適用しない。日時取得・設定に失敗した場合も、未公開出力だけを
解放して元書庫を保持する。更新日時と参照日時には、この作成日時の処理を適用しない。

一覧 `l/v` の `-jn1` は、列挙通知を先に行い、基準ディレクトリ（省略時は CWD）に存在する項目を
表示から除外する。通知の選択結果と、存在確認後の最終選択結果は別に保持し、互換ログの再構築にも
最終選択を渡す。名前だけの表示では最後の存在確認失敗を Win32 エラーへ反映し、命令開始時に初期化する。

## 不変条件と採用済みの判断

- **バイナリ互換を優先する。** x86、stdcall、1 バイト境界の公開構造体、原版の名前・序数とバージョンリソースを維持する。これにより既存ホストとの接続を保つ一方、型幅や API 形状の自由な変更には制約がある。
- **既存 LHa 系コードを組み込む。** UnLha64x 由来のラッパーと LHa エンジンを拡張している。原版 DLL は比較対象であり製品のリンク先ではない。既存コードを活用する代わりに派生元の配布条件を引き継ぐ。詳細は [第三者告知](UnLha32Re/THIRD_PARTY_NOTICES.md) に集約する。
- **データ保護を互換性より優先する条件がある。** 未格納入力や拒否された既存項目の保持、破損しない LH3 出力などは意図的な差異であり、原版との単純一致ではなく保持・復元の試験で保証する。例外の一覧はプロジェクト README を参照する。
- **API ごとのエラー契約を維持する。** メモリ展開はデータ CRC 不一致を必ずしもエラーにしないため、その成功だけでは整合性を保証できない。CRC 検査は `UnlhaCheckArchive` の契約で別途扱う。
- **組み込みエンジンはホストのシグナル処理を変更しない。** `LHA_LIBRARY` ビルドでは CLI 用の SIGINT/SIGHUP ハンドラー登録・無視を除外し、中断は DLL の通知経路で扱う。ホストプロセス全体に及ぶ副作用を避けるための境界である。
- **不正な復号表を使用前に拒否する。** Huffman 表の符号長・反復数・木の添字を検査し、表の充足度は桁あふれしない幅で判定する。不正入力を通常のエラー経路へ戻すことで、配列外参照を防ぐ。
- **SFX は展開エンジンを内包する。** ローダー、書庫、DLL と位置・サイズを持つフッターを使い、実行時にフッターの範囲を検査して DLL を一時ファイルへ取り出す。`UnlhaW` で自身の書庫を展開後、DLL を解放して一時ファイルを削除する。外部 DLL の配置に依存しない代わりに一時ファイルの作成・ロードが必要となる。
- **試験の表示と設定を隔離する。** `DesktopRunner` と互換試験プログラムは非表示デスクトップを使い、分離失敗時は停止する。ダイアログを含む実動作を検証できる一方、これはファイルやネットワークを制限するサンドボックスではない。

## ビルドと検証の位置付け

`UnLhaRe.sln` は DLL、SFX ローダー、互換試験、DesktopRunner の C++ プロジェクトをまとめる。DLL はビルド時に SFX プロジェクトを参照する。生成物は `artifacts/`、中間生成物は `build/` に分離される。

`scripts/test.ps1` はビルド、PE・エクスポート・バージョン検査、書庫 fixture に対する API 比較と機能別試験を組み合わせる。比較元 DLL と一部 fixture は追跡対象外の `sample/` に依存する。固定された期待値を使う試験とライブ比較は別に扱われる。実行コマンドと作業時の検証規約は [AGENTS.md](AGENTS.md)、隔離実行は [検証用デスクトップ](UnLha32Re/docs/testing-desktop.md) を参照する。

`test.ps1 -Profile Release` は `release.ps1 -CheckOnly` へ接続し、ビルドと `test-release.ps1` の候補単独スモークを実行する。既定の `Full` は従来の比較行列を維持する。リリースではビルド・署名・スモーク・ZIPを直列に実行し、スモーク全体の時間上限を `DesktopRunner` に渡す。表示先とHKCUの分離、DWM監視、本文照合・CRC・元データ保持の判定は短時間経路でも有効である。合格したDLLのSHA256とGit HEADを記録し、ZIP作成時に同じ署名済みDLL・コミットか検査する。毎回新しい検証を行い、過去の成功を自動で流用しない。公開手順と省略する検証範囲は [リリース手順](UnLha32Re/docs/releasing.md) を参照する。

標準入口の `invoke-dwm-monitored-test.ps1` は、隔離試験とは別プロセスで `dwm-monitor.ps1` を動かす。
監視は同じセッションの DWM の CPU 差分を記録し、試験終了後も計測して持続負荷を検出する。
高負荷時は実行固有の WPR セッションで CPU スタックを採取し、計測不能・採取不能を区別する。
監視結果は `build/dwm-monitor/` に残し、高負荷・監視不能を試験成功として扱わない。
